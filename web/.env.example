# ClawChat 環境變數配置
# 複製此文件為 .env 並填入實際值

# Gateway 訪問令牌 (必填)
GATEWAY_TOKEN=your_gateway_token_here

# 服務端口 (可選，預設 8093)
PORT=8093

# Gateway URL (可選，預設 http://127.0.0.1:18789)
GATEWAY_URL=http://127.0.0.1:18789

# OpenClaw 配置路徑 (可選)
OPENCLAW_CONFIG_PATH=~/.openclaw/openclaw.json

# API Key (可選，用於保護敏感 API 端點)
# 設置後，訪問 /api/channels, /api/config 等需要攜帶此 Key
# 用法: Authorization: Bearer YOUR_API_KEY
API_KEY=your_api_key_here

# CORS 允許的來源 (可選，逗號分隔，預設 localhost)
# 生產環境可設為實際域名，如: https://yourdomain.com
# 設為 * 允許所有來源 (不推薦)
CORS_ORIGINS=localhost,127.0.0.1
